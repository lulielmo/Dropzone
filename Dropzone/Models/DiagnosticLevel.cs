namespace Dropzone.Models;

/// <summary>
/// Severity of a script diagnostic shown in the result UI (not pasted into Medius).
/// </summary>
public enum DiagnosticLevel
{
    Info = 0,
    Warning = 1,
    Error = 2
}
